These fileOuts should match the Changes snippets in the tutorial.

Each snapshot corresponds a to loading the set of changes in sequence up to any point in the tutorial.

The snapshots may differ slightly from the original fileOuts as we fix some minor issues.

NB: before filing in these snapshots, first create the package VisualisationReview:

```
Smalltalk packageOrganizer ensurePackage: 'VisualisationReview'
```
